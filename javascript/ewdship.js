$(document).ready(function() {
	$(document).getElementById ( "shipping" ) {
		function totalPrice(subtotal, shipping) {
			$(document).getElementById( "ship") {
				function ewdShip(subtotal, shipping) {
					var subtotal;  // shouldn't this not have a number since its called above?
					var shipping  = 5.99;
					var totalPrice = subtotal + shipping;
					console.log(  );
		} // end ship
	} // end
